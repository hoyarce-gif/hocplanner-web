[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Expose functions under /api/* for cleaner URLs
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
